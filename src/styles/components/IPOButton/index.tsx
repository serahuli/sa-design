import React from 'react';
import { _getRandomAvatarColor } from '../../../utils/_getAvatar';

export interface Button {
	type: '',
  background: '',
  size: '',
  
}

export const Button = () => {
	return (
    <button></button>
	);
};
